from django.contrib import admin

# Note: No models to register in authapp.
# User authentication uses Django's built-in User model.
# Role-based profiles (Student, Teacher, Admin) are in lms_cors app.
